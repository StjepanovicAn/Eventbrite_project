<!DOCTYPE html>
<html>
<head>

	

</head>
<body>
	<h1>Evenements à venir</h1>
	<p> <%@events.each do |event|%>
		<%= @event = link_to "#{event.title}", event_path(event.id)%>
		<%end%>

	</p>

</body>
</html>